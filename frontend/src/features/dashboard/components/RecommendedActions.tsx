import { Button } from '../../../shared/components/ui/Button'
import { Card } from '../../../shared/components/ui/Card'
import styles from './dashboard.module.css'
const recommendations = [{ title: 'Review Neural Networks', description: 'Your recent answers show difficulty here.' }, { title: 'Take a quiz', description: 'Test your understanding with a quick practice set.' }]
export function RecommendedActions() { return <Card className={styles.panel}><div className="mb-4"><h2 className="mb-1">Recommended next steps</h2><p className="m-0 text-xs text-[var(--color-text-muted)]">A small action is enough to keep moving.</p></div><div className={styles.recommendations}>{recommendations.map((recommendation) => <div className={styles.recommendation} key={recommendation.title}><span className={styles.recommendationIcon} aria-hidden="true">✦</span><div className={styles.recommendationCopy}><h3>{recommendation.title}</h3><p>{recommendation.description}</p></div><Button variant="secondary" type="button">{recommendation.title === 'Take a quiz' ? 'Start' : 'Review'}</Button></div>)}</div><p className="mt-3 mb-0 text-[.68rem] text-[var(--color-text-muted)]">Demo recommendations until recommendation data is available.</p></Card> }
