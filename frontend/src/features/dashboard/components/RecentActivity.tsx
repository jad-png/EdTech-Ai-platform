import { Card } from '../../../shared/components/ui/Card'
import styles from './dashboard.module.css'
const activity = [{ icon: '✓', title: 'Completed a quiz', meta: 'Learning Theory · 2h ago', score: '82%' }, { icon: '?', title: 'Asked a question', meta: 'Cognitive Science · Yesterday' }, { icon: '✦', title: 'Kept your streak alive', meta: '3 days in a row · Yesterday' }]
export function RecentActivity() { return <Card className={styles.panel}><div className="mb-3 flex items-center justify-between"><h2 className="mb-0">Recent activity</h2><span className="text-xs text-[var(--color-text-muted)]">This week</span></div><div className={styles.activity}>{activity.map((item) => <div className={styles.activityItem} key={`${item.title}-${item.meta}`}><span className={styles.activityIcon} aria-hidden="true">{item.icon}</span><div><p className={styles.activityTitle}>{item.title}</p><p className={styles.activityMeta}>{item.meta}</p></div>{item.score && <span className={styles.activityScore}>{item.score}</span>}</div>)}</div><p className="mt-3 mb-0 text-[.68rem] text-[var(--color-text-muted)]">Demo activity until an activity feed is available.</p></Card> }
