import type { CSSProperties } from 'react'
import { Card } from '../../../shared/components/ui/Card'
import styles from './dashboard.module.css'
export function ConceptPerformance({ correct, incorrect }: { correct: number; incorrect: number }) { const total = correct + incorrect; const accuracy = total ? Math.round(correct / total * 100) : 0; return <Card className={styles.panel}><h2>Answer performance</h2><div className={styles.performance}><div className={styles.accuracy}><div className={styles.ring} style={{ '--accuracy': `${accuracy}%` } as CSSProperties} data-value={`${accuracy}%`} /><div className={styles.performanceCopy}><p>Accuracy across your completed quiz answers.</p></div></div><div className={styles.segments}><div className={styles.segmentLabel}><span>Correct answers</span><strong>{correct}</strong></div><div className={styles.segment}><div className={styles.correct} style={{ width: `${total ? correct / total * 100 : 0}%` }} /></div><div className={styles.segmentLabel}><span>Incorrect answers</span><strong>{incorrect}</strong></div><div className={styles.segment}><div className={styles.incorrect} style={{ width: `${total ? incorrect / total * 100 : 0}%` }} /></div></div></div></Card> }
