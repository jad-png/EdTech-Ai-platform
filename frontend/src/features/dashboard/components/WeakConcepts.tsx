import { Card } from '../../../shared/components/ui/Card'
import styles from './dashboard.module.css'
const concepts = [{ name: 'Neural Networks', score: 42 }, { name: 'Backpropagation', score: 56 }, { name: 'Embeddings', score: 61 }]
export function WeakConcepts() { return <Card className={styles.panel}><div className="mb-4 flex items-start justify-between"><div><h2 className="mb-1">Concepts to revisit</h2><p className="m-0 text-xs text-[var(--color-text-muted)]">Practice areas needing a little more attention.</p></div><span aria-hidden="true">↗</span></div><div className={styles.concepts}>{concepts.map((concept) => <div className={styles.concept} key={concept.name}><span className={styles.conceptName}>{concept.name}</span><span className={styles.conceptScore}>{concept.score}%</span><div className={styles.conceptBar}><span style={{ width: `${concept.score}%` }} /></div></div>)}</div><p className="mt-3 mb-0 text-[.68rem] text-[var(--color-text-muted)]">Demo focus areas until concept analytics are available.</p></Card> }
