import { Dropdown } from '../../../shared/components/ui/Dropdown'
import styles from './documents.module.css'
export function DocumentActions({ onOpen, onRename, onDelete, onGenerateFlashcards, onQuiz, onChat }: { onOpen?: () => void; onRename?: () => void; onDelete?: () => void; onGenerateFlashcards?: () => void; onQuiz?: () => void; onChat?: () => void }) { return <div className={styles.actionArea}><div className={styles.quickActions}><button className={styles.quickAction} type="button" onClick={onGenerateFlashcards}>Generate Flashcards</button><button className={styles.quickAction} type="button" onClick={onQuiz}>Quiz Me</button><button className={styles.quickAction} type="button" onClick={onChat}>Chat with Doc</button></div><Dropdown label="More document actions" trigger={<span className={styles.overflowIcon} aria-hidden="true">⋮</span>}><button className={styles.menuItem} type="button" role="menuitem" onClick={onOpen}>Open</button><button className={styles.menuItem} type="button" role="menuitem" onClick={onRename}>Rename</button><button className={`${styles.menuItem} ${styles.deleteItem}`} type="button" role="menuitem" onClick={onDelete}>Delete</button></Dropdown></div> }
